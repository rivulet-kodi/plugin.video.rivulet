"""Pure-Python Stremio protocol layer.

Everything under lib.stremio (and lib.store) is importable with plain
python3 stdlib + requests - no `xbmc*` modules. Kodi-facing code lives in
lib.ui, lib.service_runner, default.py and service.py instead.
"""
